apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Gateway API v1.5.1 (experimental channel)
# Includes standard + experimental CRDs (BackendTLSPolicy, TCPRoute, TLSRoute, UDPRoute, etc.)
resources:
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.5.1/experimental-install.yaml
